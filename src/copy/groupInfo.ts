/** Copy for the click-to-open info popovers on the three-point-estimate / phase /
 *  range / participant-estimates groups — shared by every screen that renders one
 *  of these groups (Workspace's ActiveItemPanel and LiveFacilitatorPanel,
 *  ParticipantEstimateView's EstimateForm and RevealedPanel). */

export const THREE_POINT_ESTIMATE_INFO =
  "Best, most likely, and worst case are combined using McConnell's three-point estimation to produce an expected value and a range wide enough to be right 90% of the time."

export const PHASE_INFO =
  'Where you are in the project lifecycle affects how much uncertainty is baked into the estimate — early phases get a wider cone, later phases a narrower one.'

export const RANGE_INFO =
  'The range bar shows the expected value and the 90% confidence interval (CI90) computed from your three-point estimate and phase.'

export const PARTICIPANT_ESTIMATES_INFO =
  "Each teammate submits their own best/likely/worst independently. Values stay hidden until you reveal the round, so no one anchors on someone else's number."
